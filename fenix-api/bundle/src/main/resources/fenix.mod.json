{
  "schema": 1,
  "id": "fenix-api",
  "version": "${version}",
  "name": "Fenix API",
  "description": "Every Fenix API module in a single dependency.",
  "authors": ["D4EMON"],
  "license": "Apache-2.0",
  "contact": {
    "homepage": "https://github.com/D4EMONDEV/Fenix",
    "issues": "https://github.com/D4EMONDEV/Fenix/issues"
  },
  "side": "both",
  "depends": {
    "fenix": ">=0.1.0",
    "minecraft": "~${minecraft_version}",
    "fenix-api-event": "${version}",
    "fenix-api-registry": "${version}",
    "fenix-api-resource": "${version}",
    "fenix-api-network": "${version}",
    "fenix-api-command": "${version}",
    "fenix-api-config": "${version}"
  }
}
