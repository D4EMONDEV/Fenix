# Expanded at build time; read by fr.d4emon.fenix.gradle.FenixDevPlugin.
#
# Only tool versions live here — things with no tie to a Minecraft version and
# no place in a mod's dependency graph.
#
# The Fenix coordinates used to sit here too, one baked line each. They have
# moved to platforms.json, which carries every game version rather than the one
# this plugin happened to be built against; see fr.d4emon.fenix.gradle.Platforms
# for why that mattered.
vineflower=${vineflower_version}
