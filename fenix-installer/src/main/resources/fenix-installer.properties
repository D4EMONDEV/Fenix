# Expanded at build time; read by fr.d4emon.fenix.installer.InstallerMain.
version=${version}
minecraft=${minecraft_version}

# Which Minecraft versions this installer can install into, newest first.
# One today: the payload it carries is built against a single game version.
# A build carrying more would list them here, and the window would offer them.
supported=${minecraft_version}
